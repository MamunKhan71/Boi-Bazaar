import Navbar from "./Navbar.jsx";

const MainContainer = () => {
    return (
        <div className="container mx-auto">
            <Navbar></Navbar>
        </div>
    );
};

export default MainContainer;